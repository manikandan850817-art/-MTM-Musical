# MTM Musical
A starter Android Java project for an AI music app.

## Screens
Splash, Login, Register, Home, Create Music, Library, Favorites, Downloads, Profile, Settings.

## Next
Connect a real music-generation API inside `api/` and persist songs/favorites in `database/`.
